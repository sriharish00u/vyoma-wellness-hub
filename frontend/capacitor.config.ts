import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vyoma.wellnesshub',
  appName: 'vyoma-wellness-hub',
  webDir: 'dist'
};

export default config;
